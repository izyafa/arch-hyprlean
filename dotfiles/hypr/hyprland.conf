monitor = WL-1, 1152x648, 0x0, 1 # this is for testing in 1366x768 display, later will be "monitor = , preferred, auto, 1" if ok

$mainMod = ALT # after testing, change to SUPER

bind = $mainMod, Q, exec, foot --config=/home/panji/arch-hyprlean/dotfiles/foot/foot.ini # i havent figure out how to use this config as default
bind = $mainMod, M, exit
bind = $mainMod, C, killactive

general {
  col.active_border = rgb(ff0000) # red for testing
  border_size = 1
  }
